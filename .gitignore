# Only the files the website actually serves are tracked (index.html, styles.css,
# script.js, pres-map.js, assets/, files/, CNAME, .nojekyll).
# Everything else — CV source, build script, reference material, notes — stays local.
cv/
README.md
